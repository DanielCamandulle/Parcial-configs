server:
  port: 8080

spring:
  application:
    name: gateway
      
   spring:
    cloud:
        gateway:
            mvc:
                routes:
                    - id: catalogRoute
                      uri: lb://catalog-service/
                      predicates:
                        - Path=/catalog/**
                        
                        
                        
  

